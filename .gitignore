# Dependencias
node_modules/
.pnpm-store/

# Build de Astro
dist/
.astro/

# Entorno y secretos (NUNCA subir)
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# SO / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Backups de edición
*.bak

# Dumps de base de datos (servicio backup del compose)
backups/
*.dump

# Vault de resultados del worker (cartas, CV, TABLERO, CSV) — datos personales, NUNCA subir
salidas/
vault/

# Temporales del montaje (FUSE) — no subir
.fuse_hidden*
